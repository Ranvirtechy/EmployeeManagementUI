// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  applicationROOTDomain: 'http://dev.office.thefoodrunners.com/',
  internalApiRoot: 'https://localhost:44364/api/',
  siteRootURL : 'http://dev.operation.thefoodrunners.com'
  
  //internalApiRoot: 'http://dev.operation.thefoodrunners.com/api/v1/',
  //roor_api : 'https://localhost:44364/api/'

    // applicationROOTDomain: 'http://office.thefoodrunners.com/',
    // internalApiRoot: 'http://api.office.thefoodrunners.com/api/v1/',
    // siteRootURL : 'http://office.thefoodrunners.com'
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
